import { ComunidadReconocida } from './comunidad-reconocida';

describe('ComunidadReconocida', () => {
  it('should create an instance', () => {
    expect(new ComunidadReconocida()).toBeTruthy();
  });
});
